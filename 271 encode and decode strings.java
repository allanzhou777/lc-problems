class Solution {
    public String encode(List<String> strs) {
        // not sure what exactly the problem is? 
        // Can I just append a series of characters after each string to encode it
        // like "String 1" + "abcdefghijklmnopqrs...z" + "String 2" + "abcd..."
    }

    public String decode(String s) {
        // same as above, can I just find the next available sequence that has the 256 distinct characters
    }
}